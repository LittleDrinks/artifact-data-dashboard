# 版本历史 (Version History)

## v0.0 - "Genesis" (创世版本)

**发布日期**: 2026-02-15
**代号**: 第0版本 (Genesis Edition)

### 版本说明

这是 **Artifact Data Dashboard** 的**第0版本**，标志着项目基础架构的确立和代码库的初始整理。

#### 为什么是第0版本？

第0版本代表项目的**起点**——一个干净、有序的基础，为后续的功能开发奠定坚实根基。在这个版本中，我们专注于：

- ✅ **代码结构规范化** - 清晰的前后端分离
- ✅ **文档体系建立** - 完整的PRD、MRD和技术规范
- ✅ **开发环境配置** - Docker Compose一键启动
- ✅ **基础功能完整** - 文物管理、知识图谱、AI问答核心功能就绪

### 主要里程碑

#### 架构确立
- **前端**: React 18 + Ant Design 5 + D3.js/ECharts
- **后端**: Express 4 + Node.js + MySQL/Neo4j/Redis
- **AI**: Ollama本地大模型 + MCP工具调用
- **部署**: Docker Compose开发/生产双环境

#### 核心功能模块
1. **文物数据管理** - Excel导入导出、CRUD操作
2. **知识图谱可视化** - Neo4j图数据库 + D3.js力导向图
3. **AI智能问答** - MCP架构 + 流式响应 + Markdown渲染
4. **附件管理系统 (DAMS)** - 文件夹管理、标签系统、缩略图
5. **统计看板** - 词云、分类统计、年代分布

### 项目统计

| 指标 | 数据 |
|------|------|
| **总代码行数** | ~20,000+ 行 |
| **前端代码** | ~8,000 行 (React) |
| **后端代码** | ~12,000 行 (Node.js) |
| **测试覆盖** | 核心服务覆盖 |
| **文档页数** | 50+ 页 |

### 功能测试结果

**测试日期**: 2026-02-15  
**测试环境**: Docker Compose 开发环境  
**测试账号**: admin/admin123 (已写入 AGENTS.md)

| 功能模块 | 测试状态 | 备注 |
|----------|----------|------|
| 登录系统 | ✅ 通过 | 使用 admin/admin123 成功登录 |
| 数据大屏 | ✅ 通过 | 显示文物统计：18件文物、15件已数字化 |
| 关键词搜索 | ✅ 通过 | 搜索"青铜"返回6条结果（越王勾践剑、四羊方尊等） |
| 知识图谱 | ✅ 通过 | 页面加载正常，支持力导向图参数调整 |
| 智能问答 | ✅ 通过 | 三种模式（在线/本地/模拟）可切换，支持Markdown渲染 |
| 附件管理 | ✅ 通过 | 支持文件上传、目录导入、Excel关联导入 |
| 词云分析 | ✅ 通过 | 页面加载正常 |
| 个人信息 | ✅ 通过 | 页面加载正常 |

**测试截图**: `test-result-dashboard.png`

### 已知问题

- 前端页面组件需要进一步拆分（Chat.js 706行、Attachments.js 757行）
- 后端services命名需要统一规范
- Neo4j工具文件存在重复
- 知识图谱搜索需要实际数据支持（当前显示"暂无图谱数据"）
- 部分文物图片加载失败（404错误）

### 架构演进方向（v0.1+）

**核心转型：从分散页面到工作台中心**

基于用户反馈和技术评估，系统将从"多页面分散架构"演进为"工作台为中心的集成架构"。

#### 新架构核心卖点

| 卖点 | 说明 |
|------|------|
| **一键部署** | `docker-compose up` 5分钟启动完整系统，内置示例数据 |
| **功能集成** | 搜索→探索→修复→问答在同一界面完成，无需页面切换 |
| **工作台中心** | 左侧面板（文物探索）+ 右侧面板（AI助手），组件实时联动 |

#### 工作台架构设计

```
┌─────────────────────────────────────────────────────────────┐
│                    统一工作台 (Workbench)                      │
├───────────────────────────┬─────────────────────────────────┤
│      左侧：探索与发现        │       右侧：智能助手             │
│  ┌─────────────────────┐  │  ┌───────────────────────────┐  │
│  │   文物搜索           │  │  │    AI 智能问答            │  │
│  │   - 关键词检索       │  │  │    - 自然语言查询         │  │
│  │   - 图片浏览         │  │  │    - 知识图谱问答         │  │
│  └─────────────────────┘  │  └───────────────────────────┘  │
│  ┌─────────────────────┐  │  ┌───────────────────────────┐  │
│  │   知识图谱可视化      │  │  │    AI 图像修复            │  │
│  │   - 力导向图         │  │  │    - 破损图像上传         │  │
│  │   - 实体关联         │  │  │    - 修复预览             │  │
│  └─────────────────────┘  │  └───────────────────────────┘  │
└───────────────────────────┴─────────────────────────────────┘
```

#### 阶段规划

**Phase 1 (v0.1) - 工作台框架**
- 创建 WorkbenchLayout 组件
- 实现可拖拽分隔线
- 左右面板占位符和模式切换
- 路由重构：`/workbench` 为主入口

**Phase 2 (v0.2) - 左侧探索区**
- SearchPanel 组件封装（迁移现有搜索功能）
- GraphPanel 组件封装（迁移现有图谱可视化）
- 选中状态共享机制
- 分屏模式实现

**Phase 3 (v0.3) - 右侧AI助手**
- ChatPanel 组件封装（迁移现有AI问答）
- InpaintPanel 组件实现（图像修复）
- 阿里云API后端集成
- 左右上下文联动（选中→AI感知）

**Phase 4 (v0.4) - 集成优化**
- URL状态持久化（刷新不丢失）
- 性能优化（虚拟列表、懒加载）
- 移动端适配（面板堆叠）
- 快捷键支持

**Phase 5 (v0.5) - 遗留迁移**
- 旧路由重定向到工作台
- 删除废弃组件
- 更新用户引导

#### 参考文档

- 新架构设计概要：`docs/ARCHITECTURE-v2-workbench.md`
- 系统概述（新）：`specs/01-system-overview/spec.md`
- 前端规格（新）：`specs/05-frontend/spec.md`
- AI MCP规格（新）：`specs/06-ai-mcp/spec.md`
- 工作分解（新）：`docs/WBS.md`

#### 长期目标

- [ ] 工作台架构完整落地
- [ ] 图像修复功能集成
- [ ] 百度百科数据迁移
- [ ] 引入TypeScript类型安全
- [ ] 完善测试覆盖率

---

## 版本命名规则

我们采用 **语义化版本控制** (SemVer) 结合 **代号系统**：

```
v主版本.次版本.修订版本 - "代号"
```

- **主版本 (Major)**: 不兼容的API更改
- **次版本 (Minor)**: 向后兼容的功能添加
- **修订版本 (Patch)**: 向后兼容的问题修复
- **代号**: 每个大版本使用一个主题代号（如文物、朝代、工艺等）

### 代号规划

| 版本 | 代号 | 主题 |
|------|------|------|
| v0.0 | Genesis | 创世/奠基 |
| v0.1 | Bronze | 青铜时代 |
| v0.2 | Porcelain | 瓷器文明 |
| v1.0 | SilkRoad | 丝绸之路 |

---

*Last updated: 2026-02-15 by Sisyphus Agent*
